{
  "name": "discover",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "deploy-testing": "serverless deploy -v --stage testing --SENDGRID_API_KEY ${SENDGRID_API_KEY} --ASMALLTALK_EMAIL ${ASMALLTALK_EMAIL}",
    "deploy-staging": "serverless deploy -v --stage staging --SENDGRID_API_KEY ${SENDGRID_API_KEY} --ASMALLTALK_EMAIL ${ASMALLTALK_EMAIL}",
    "deploy-production": "serverless deploy -v --stage production --SENDGRID_API_KEY ${SENDGRID_API_KEY} --ASMALLTALK_EMAIL ${ASMALLTALK_EMAIL}",
    "test": "DYNAMODB_TABLE=asmalltalk-api-testing jest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@octokit/rest": "^15.2.7",
    "@sendgrid/mail": "^6.2.1",
    "aws-sdk": "^2.238.1",
    "snoowrap": "^1.15.2"
  },
  "devDependencies": {
    "jest": "^22.4.3",
    "serverless": "^1.27.2"
  },
  "jest": {
    "globals": {
      "__DEV__": true,
      "test": true,
      "expect": true,
      "describe": true
    }
  }
}
